# Instructions Claude

- Lire `AI_COLLABORATION.md`.
- Avant tout code métier, fetch `ai/coordination`, lire le dernier `AI_HANDOFF.md`, vérifier les LOCK, poser puis pousser son LOCK; un LOCK local n'est pas valide.
- Développer uniquement sur une branche `claude/*`; réserver `ai/coordination` aux LOCK, handoffs et états de lots.
- Après tests et push du travail, mettre à jour le handoff, retirer le LOCK et pousser `ai/coordination`.
- Ne jamais pousser directement vers `main` ni y fusionner sans l'autorisation explicite de Blaise; l'autorisation d'un lot ne vaut pas autorisation de fusion.
- `push --force` est interdit; supprimer une branche distante exige l'autorisation de Blaise.
